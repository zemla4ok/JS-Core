const dmitry = new Person('Dmitry', 'Kotovich');
const currentDate = new Date();
const numbers = [1, 1, 3, 5, 234, 123];
const uniqueNumbers = new Set(numbers);

function log(value) {
  console.log(value);
}

const calcSum = (x, y) => x + y;